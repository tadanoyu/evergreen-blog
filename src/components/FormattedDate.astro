---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={new Date(date).toISOString()}>
	{
		new Date(date).toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
			hour: '2-digit',
			minute: '2-digit',
		})
	}
</time>
